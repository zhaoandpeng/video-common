/*define(["tool/Utils", "view/videoOrderPasswordView"], function (utils, view) {
  function bindEvents() {
    var bindings = [
    ];
    utils.bindEvents(bindings);
  }

  function showPage(){
    //3秒钟以后进入视频验证页面
    setTimeout(function(){
      window.location.href = "videoOrder-recording.html";
    },3000);

  }

  function init() {
    bindEvents();
    showPage();
  }

  return {
    init: init
  }
});
*/