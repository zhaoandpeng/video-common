/*define(["tool/Utils", "view/videoOrderRecordingPasswordView"], function (utils, view) {
  function bindEvents() {
    var bindings = [
    ];
    utils.bindEvents(bindings);
  }

  function showPage(){
    //3秒钟以后到失败页面
    setTimeout(function(){
      window.location.href = "videoOrder-fail.html";
    },3000);
  }

  function init() {
    bindEvents();
    showPage();
  }

  return {
    init: init
  }
});
*/