/*define(["tool/Utils", "view/videoOrderFailView"], function (utils, view) {
  function bindEvents() {
    var bindings = [{
      "element": ".edit-btn",
      "event":"click",
      "handler":goEdit
    }
    ];
    utils.bindEvents(bindings);
  }

  function goEdit(){
    location.href = 'openAccount.html';
  }

  function init() {
    bindEvents();
  }

  return {
    init: init
  }
});
*/